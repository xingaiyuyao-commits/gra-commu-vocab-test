# 単語テスト再実装 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Railway外部リンクだった「単語テスト」を、このプロジェクト内にSocket.IOゲームとして再実装する（穴埋め入力式・各自5問→提出・全員提出後ランキング）。

**Architecture:** 既存のコインタワー/並べ替えバトルと同じ構成。`server.js`に`quiz:*`イベント群と独立した`quizRooms`を追加、問題データはPDFから抽出した静的モジュール（カテゴリ別3ファイル＋インデックス）、クライアントは`public/quiz.html`1ファイル。

**Tech Stack:** Node.js + Express + Socket.IO 4（既存のまま）。テストは`node`直実行のスクリプト（`socket.io-client`はdevDependenciesに導入済み）。

**Spec:** `docs/superpowers/specs/2026-07-19-word-quiz-redesign-design.md`

## Global Constraints

- ゲームルールは元と同じ: 穴埋め**入力式**・自分のペースで**5問**→提出・全員提出後にランキング発表（正解数降順→提出の早さ順）
- 収録セット: Clacel 2.0（Series 1〜8）/ IELTS（Series 1〜5）/ TOEIC（Series 1〜5）＝計18セット。各セット20語だが**Clacel Series 8のみ元PDFが12語構成**なので12語で収録する。**英検は収録しない**
- `answer`は例文中の活用形をそのまま小文字で保持。判定は「両者を小文字化・trimして完全一致」（既存コインタワーと同じ）
- UIはcoin.html / order.htmlと同じデザイントークン（CSS変数・カード・ボタン）を踏襲。モバイルファースト・1画面1問
- 既存のコインタワー/並べ替えバトル/Surviveカードのコード・リンクには触らない
- コミットメッセージは日本語・既存スタイル（例: `並べ替えバトル追加: …`）に合わせる

## PDFソースの場所（データ抽出タスクで使用）

| カテゴリ | フォルダ | ファイル |
|---|---|---|
| Clacel 2.0 | `/Users/tina/Desktop/単語テスト/単語テスト_Clacel2.0提案用/` | `Clacel_02_Series01.pdf` 〜 `Clacel_02_Series08.pdf` |
| IELTS | `/Users/tina/Desktop/単語テスト/単語テスト_IELTS提案用/` | `IELTS_Series01.pdf` 〜 `IELTS_Series05.pdf` |
| TOEIC | `/Users/tina/Desktop/単語テスト/単語テスト_TOIEC提案用/`（フォルダ名はTOIEC表記） | `TOEIC_Series01.pdf` 〜 `TOEIC_Series05.pdf` |

各PDFの構成: 前半ページが「Word List」（1〜20の番号付きで、**見出し語**・発音記号・品詞・英語定義・**日本語の意味**・**例文**（対象単語が太字斜体））。Readツールの`pages: "1-2"`で読む。20語に満たなければ`pages: "3"`も読む（Word Listの後の「Test — Part 1」ページは使わない）。

### 抽出ルール（全データタスク共通）

各エントリから1問を作る:

- `sentence`: 例文の太字斜体の単語を`___`に置換した文（同じ語が2回出る場合は強調されている方だけ置換）
- `answer`: 置換した単語そのもの（例文中の活用形のまま）を小文字化。例: 例文が "The children **pretended** to be superheroes." なら `"pretended"`
- `base`: 見出し語を小文字化（例: `"pretend"`）
- `hint`: `answer`の頭文字＋残り文字数分の`_`。例: `pretended` → `"p________"`（長さ9）
- `ja`: 日本語の意味の行をそのまま（例: `"ふりをする"`）

---

### Task 1: 検証スクリプト＋Clacel 2.0データ（8シリーズ）

**Files:**
- Create: `scripts/validate-wordtests.js`
- Create: `wordtests-clacel.js`

**Interfaces:**
- Produces: `wordtests-clacel.js` は `module.exports = { label: "Clacel 2.0", series: [{ name: "Series 1", items: [{ sentence, answer, base, hint, ja }] }, …] }`（series 8個。items はSeries 1〜7が20件、Series 8のみ元PDFが12語構成のため12件）
- Produces: `scripts/validate-wordtests.js` は `node scripts/validate-wordtests.js <ルートからの相対ファイル名> <シリーズごとの期待語数カンマ区切り>` で実行し、不正があれば非0で終了（例: `node scripts/validate-wordtests.js wordtests-clacel.js 20,20,20,20,20,20,20,12`）

- [ ] **Step 1: 検証スクリプトを書く**

```js
// wordtests-*.js の構造検査。使い方: node scripts/validate-wordtests.js wordtests-clacel.js 20,20,20,20,20,20,20,12
// 第2引数はシリーズごとの期待語数（カンマ区切り。個数＝期待シリーズ数）
const path = require("path");
const file = process.argv[2];
const expectedCounts = (process.argv[3] || "").split(",").map(Number);
const data = require(path.resolve(__dirname, "..", file));

const errors = [];
if (!data.label) errors.push("label がありません");
if (!Array.isArray(data.series) || data.series.length !== expectedCounts.length)
  errors.push(`series 数が ${expectedCounts.length} ではありません: ${data.series ? data.series.length : 0}`);

(data.series || []).forEach((s, si) => {
  if (!s.name) errors.push(`series[${si}]: name がありません`);
  if (!Array.isArray(s.items) || s.items.length !== expectedCounts[si])
    errors.push(`${s.name}: items が${expectedCounts[si]}件ではありません (${s.items ? s.items.length : 0})`);
  const seen = new Set();
  (s.items || []).forEach((it, i) => {
    const tag = `${s.name} #${i + 1}`;
    if (!it.sentence || !it.sentence.includes("___")) errors.push(`${tag}: sentence に ___ がありません`);
    if (!it.answer || it.answer !== it.answer.toLowerCase()) errors.push(`${tag}: answer が小文字ではありません`);
    if (!/^[a-z][a-z'-]*$/.test(it.answer || "")) errors.push(`${tag}: answer の形式が不正: ${it.answer}`);
    if (!it.base || it.base !== it.base.toLowerCase()) errors.push(`${tag}: base が小文字ではありません`);
    if (!it.hint || it.hint.length !== it.answer.length || it.hint[0] !== it.answer[0])
      errors.push(`${tag}: hint が answer（活用形）と対応していません`);
    if (!it.ja) errors.push(`${tag}: ja がありません`);
    if (seen.has(it.base)) errors.push(`${s.name}: base 重複 ${it.base}`);
    seen.add(it.base);
  });
});

if (errors.length) {
  console.error(errors.join("\n"));
  process.exit(1);
}
console.log(`OK: ${file}（${data.series.length} series / ${data.series.reduce((a, s) => a + s.items.length, 0)}問）`);
```

- [ ] **Step 2: 失敗することを確認**

Run: `node scripts/validate-wordtests.js wordtests-clacel.js 20,20,20,20,20,20,20,12`
Expected: FAIL（`Cannot find module` でエラー終了）

- [ ] **Step 3: PDF 8ファイルを読んで `wordtests-clacel.js` を作成**

`Clacel_02_Series01.pdf` 〜 `Clacel_02_Series08.pdf` を1つずつ `pages: "1-2"` で読み、冒頭の「抽出ルール」に従って書き出す。Series 1〜7は20語、**Series 8のみ元PDFが12語構成なので12語**（捏造や重複での水増しはしない）。ファイル形式:

```js
// Clacel 2.0 単語テスト問題（PDFのWord List例文から生成）
// answer は例文中の活用形を小文字で保持。判定時に両者を小文字化して比較する
module.exports = {
  label: "Clacel 2.0",
  series: [
    {
      name: "Series 1",
      items: [
        { sentence: "The children ___ to be superheroes.", answer: "pretended", base: "pretend", hint: "p________", ja: "ふりをする" },
        { sentence: "Please ___ your shoes before entering the room.", answer: "remove", base: "remove", hint: "r_____", ja: "取り除く" },
        // …このシリーズ残り18件
      ],
    },
    // … Series 2〜8 も同様
  ],
};
```

- [ ] **Step 4: 検証を通す**

Run: `node scripts/validate-wordtests.js wordtests-clacel.js 20,20,20,20,20,20,20,12`
Expected: `OK: wordtests-clacel.js（8 series / 152問）`

- [ ] **Step 5: Commit**

```bash
git add scripts/validate-wordtests.js wordtests-clacel.js
git commit -m "単語テスト: Clacel 2.0の問題データ8シリーズと検証スクリプトを追加"
```

---

### Task 2: IELTSデータ（5シリーズ）

**Files:**
- Create: `wordtests-ielts.js`

**Interfaces:**
- Consumes: `scripts/validate-wordtests.js`（Task 1）
- Produces: `wordtests-ielts.js` — Task 1と同形式で `label: "IELTS"`、series 5個×20件

- [ ] **Step 1: 検証が失敗することを確認**

Run: `node scripts/validate-wordtests.js wordtests-ielts.js 20,20,20,20,20`
Expected: FAIL（`Cannot find module`）

- [ ] **Step 2: `IELTS_Series01.pdf`〜`IELTS_Series05.pdf` を読んで `wordtests-ielts.js` を作成**

Task 1 Step 3 と同じ形式・同じ抽出ルール。`label: "IELTS"`。

- [ ] **Step 3: 検証を通す**

Run: `node scripts/validate-wordtests.js wordtests-ielts.js 20,20,20,20,20`
Expected: `OK: wordtests-ielts.js（5 series / 100問）`

- [ ] **Step 4: Commit**

```bash
git add wordtests-ielts.js
git commit -m "単語テスト: IELTSの問題データ5シリーズを追加"
```

---

### Task 3: TOEICデータ（5シリーズ）

**Files:**
- Create: `wordtests-toeic.js`

**Interfaces:**
- Consumes: `scripts/validate-wordtests.js`（Task 1）
- Produces: `wordtests-toeic.js` — Task 1と同形式で `label: "TOEIC"`、series 5個×20件

- [ ] **Step 1: 検証が失敗することを確認**

Run: `node scripts/validate-wordtests.js wordtests-toeic.js 20,20,20,20,20`
Expected: FAIL（`Cannot find module`）

- [ ] **Step 2: `TOEIC_Series01.pdf`〜`TOEIC_Series05.pdf` を読んで `wordtests-toeic.js` を作成**

フォルダは `単語テスト_TOIEC提案用`（TOIEC表記）にある。Task 1 Step 3 と同じ形式・同じ抽出ルール。`label: "TOEIC"`。

- [ ] **Step 3: 検証を通す**

Run: `node scripts/validate-wordtests.js wordtests-toeic.js 20,20,20,20,20`
Expected: `OK: wordtests-toeic.js（5 series / 100問）`

- [ ] **Step 4: Commit**

```bash
git add wordtests-toeic.js
git commit -m "単語テスト: TOEICの問題データ5シリーズを追加"
```

---

### Task 4: インデックス `wordtests.js`

**Files:**
- Create: `wordtests.js`

**Interfaces:**
- Consumes: `wordtests-clacel.js` / `wordtests-ielts.js` / `wordtests-toeic.js`
- Produces: `require("./wordtests")` が `{ clacel: {label, series}, ielts: {…}, toeic: {…} }` を返す。サーバー（Task 5）はこのキー`clacel|ielts|toeic`をクライアントから受け取る

- [ ] **Step 1: インデックスを書く**

```js
// 単語テストの問題データ索引。キーはクライアントの quiz:startGame の category と一致させる
module.exports = {
  clacel: require("./wordtests-clacel"),
  ielts: require("./wordtests-ielts"),
  toeic: require("./wordtests-toeic"),
};
```

- [ ] **Step 2: 3カテゴリ18セットが揃っていることを確認**

Run:
```bash
node -e "const w=require('./wordtests');const n=Object.values(w).reduce((a,c)=>a+c.series.length,0);console.log(Object.keys(w).join(','), n+' sets');if(n!==18)process.exit(1)"
```
Expected: `clacel,ielts,toeic 18 sets`

- [ ] **Step 3: Commit**

```bash
git add wordtests.js
git commit -m "単語テスト: 問題データのインデックスを追加"
```

---

### Task 5: サーバー `quiz:*` イベント＋通しフローテスト

**Files:**
- Create: `scripts/test-quiz-flow.js`
- Modify: `server.js`（`const PORT = process.env.PORT || 3000;` の行の直前に追記）

**Interfaces:**
- Consumes: `wordtests.js`（Task 4）、既存の`shuffle()`（server.js内に定義済み・モジュールスコープなので再利用できる）
- Produces（クライアントTask 6が使うプロトコル）:
  - `quiz:createRoom {name}` cb `{roomCode, isHost}`
  - `quiz:joinRoom {roomCode, name}` cb `{roomCode, isHost}` または `{error}`（ロビー中のみ参加可）
  - `quiz:playersUpdate {hostId, hostName, players: [{id, name, submitted}]}`
  - `quiz:startGame {category, seriesIndex}`（ホストのみ）→ 全員に `quiz:started {setLabel, total, questions: [{sentence, hint, ja}]}`（答えは送らない）
  - `quiz:submit {answers: string[5]}` → 全員に `quiz:submitProgress {submitted, total}`
  - 全員提出で `quiz:results {ranking: [{id, name, score, total, timeMs}], review: [{sentence, answer, ja}]}`
  - `quiz:playAgain`（ホストのみ・finished時）→ 全員に `quiz:backToLobby` ＋ `quiz:playersUpdate`

- [ ] **Step 1: 通しフローテストを書く**

```js
// 単語テストのサーバーフロー検証。使い方: node scripts/test-quiz-flow.js
const { spawn } = require("child_process");
const path = require("path");
const { io } = require("socket.io-client");
const WORDTESTS = require("../wordtests");

const PORT = 3199;
const URL = `http://localhost:${PORT}`;
const connect = () => io(URL, { transports: ["websocket"] });
const results = [];
const check = (cond, msg) => { results.push(cond); console.log((cond ? "ok:" : "FAIL:"), msg); };

async function main() {
  const server = spawn("node", [path.join(__dirname, "..", "server.js")], {
    env: { ...process.env, PORT: String(PORT) },
    stdio: ["ignore", "pipe", "inherit"],
  });
  await new Promise((res) => server.stdout.on("data", (d) => { if (d.toString().includes("Server running")) res(); }));

  try {
    // --- シナリオ1: 2人で通しプレイ（正解/不正解・順位・答え合わせ） ---
    const host = connect();
    const guest = connect();

    const created = await new Promise((res) => host.emit("quiz:createRoom", { name: "ホスト" }, res));
    check(!!(created.roomCode && created.isHost), "ルーム作成でコードとホスト権が返る");

    const joined = await new Promise((res) => guest.emit("quiz:joinRoom", { roomCode: created.roomCode, name: "参加者" }, res));
    check(joined.roomCode === created.roomCode && joined.isHost === false, "参加者がロビーに入れる");

    const bad = await new Promise((res) => guest.emit("quiz:joinRoom", { roomCode: "XXXX", name: "誰か" }, res));
    check(!!bad.error, "存在しないルームコードはエラー");

    const startedBoth = Promise.all([
      new Promise((res) => host.once("quiz:started", res)),
      new Promise((res) => guest.once("quiz:started", res)),
    ]);
    host.emit("quiz:startGame", { category: "ielts", seriesIndex: 0 });
    const [qh, qg] = await startedBoth;
    check(qh.questions.length === 5 && qh.total === 5, "5問配布される");
    check(JSON.stringify(qh.questions) === JSON.stringify(qg.questions), "全員に同じ問題が配られる");
    check(!("answer" in qh.questions[0]), "配布した問題に答えが含まれない");
    check(qh.setLabel === "IELTS Series 1", "セット名が届く");

    // 正答は問題文からデータを引いて求める（ホストは全問正解、参加者は全問空欄）
    const items = WORDTESTS.ielts.series[0].items;
    const correctAnswers = qh.questions.map((q) => items.find((it) => it.sentence === q.sentence).answer);

    const resultsBoth = Promise.all([
      new Promise((res) => host.once("quiz:results", res)),
      new Promise((res) => guest.once("quiz:results", res)),
    ]);
    const progressP = new Promise((res) => guest.once("quiz:submitProgress", res));
    host.emit("quiz:submit", { answers: correctAnswers.map((a) => "  " + a.toUpperCase() + " ") });
    const prog = await progressP;
    check(prog.submitted === 1 && prog.total === 2, "提出状況 1/2 が配信される");
    guest.emit("quiz:submit", { answers: ["", "", "", "", ""] });
    const [rh] = await resultsBoth;
    check(rh.ranking.length === 2, "全員提出で結果が全員に届く");
    check(rh.ranking[0].name === "ホスト" && rh.ranking[0].score === 5, "大文字・空白混じりでも正解扱いで1位");
    check(rh.ranking[1].score === 0, "空欄は0点");
    check(rh.review.length === 5 && rh.review.every((r) => r.answer && r.sentence.includes("___")), "答え合わせ用の正答が届く");

    // 再戦: ロビーに戻れる
    const backP = new Promise((res) => host.once("quiz:backToLobby", res));
    host.emit("quiz:playAgain");
    await backP;
    check(true, "playAgainでロビーに戻る");
    host.disconnect(); guest.disconnect();

    // --- シナリオ2: プレイ中の切断で残りメンバーだけで結果発表 ---
    const h2 = connect();
    const g2 = connect();
    const c2 = await new Promise((res) => h2.emit("quiz:createRoom", { name: "A" }, res));
    await new Promise((res) => g2.emit("quiz:joinRoom", { roomCode: c2.roomCode, name: "B" }, res));
    const started2 = new Promise((res) => h2.once("quiz:started", res));
    h2.emit("quiz:startGame", { category: "toeic", seriesIndex: 2 });
    await started2;
    const results2 = new Promise((res) => h2.once("quiz:results", res));
    h2.emit("quiz:submit", { answers: ["a", "b", "c", "d", "e"] });
    g2.disconnect(); // 未提出のまま離脱
    const r2 = await results2;
    check(r2.ranking.length === 1 && r2.ranking[0].name === "A", "未提出者の切断後、残りだけで結果発表");
    h2.disconnect();
  } finally {
    server.kill();
  }
  const failed = results.filter((c) => !c).length;
  console.log(failed === 0 ? "ALL PASS" : `${failed} FAILED`);
  process.exit(failed === 0 ? 0 : 1);
}
main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: 失敗することを確認**

Run: `node scripts/test-quiz-flow.js`
Expected: FAIL（`quiz:createRoom`のcbが呼ばれずタイムアウト＝ハングするので、10秒待って進まなければCtrl-C相当で中断してよい。ハング自体が「未実装で失敗」の確認）

- [ ] **Step 3: server.js に quiz イベント群を実装**

`server.js` の `const PORT = process.env.PORT || 3000;` の直前に追記:

```js
/* ========== 単語テスト（シリーズ別5問・提出制ランキング） ========== */

const WORDTESTS = require("./wordtests");
const QUIZ_QUESTION_COUNT = 5;
const quizRooms = {}; // roomCode -> room state

function makeQuizRoomCode() {
  const chars = "ABCDEFGHJKMNPQRSTUVWXYZ23456789";
  let code = "";
  for (let i = 0; i < 4; i++) code += chars[Math.floor(Math.random() * chars.length)];
  return quizRooms[code] ? makeQuizRoomCode() : code;
}

function quizPublicPlayers(room) {
  return Object.entries(room.players).map(([id, p]) => ({
    id,
    name: p.name,
    submitted: p.submittedAt !== null,
  }));
}

function quizPlayersUpdate(roomCode) {
  const room = quizRooms[roomCode];
  io.to(roomCode).emit("quiz:playersUpdate", {
    hostId: room.host,
    hostName: room.players[room.host].name,
    players: quizPublicPlayers(room),
  });
}

function quizMaybeFinish(roomCode) {
  const room = quizRooms[roomCode];
  if (!room || room.phase !== "playing") return;
  const players = Object.values(room.players);
  if (players.length === 0 || !players.every((p) => p.submittedAt !== null)) return;
  room.phase = "finished";
  const ranking = Object.entries(room.players)
    .map(([id, p]) => ({
      id,
      name: p.name,
      score: p.score,
      total: QUIZ_QUESTION_COUNT,
      timeMs: p.submittedAt - room.startedAt,
    }))
    .sort((a, b) => b.score - a.score || a.timeMs - b.timeMs);
  io.to(roomCode).emit("quiz:results", {
    ranking,
    review: room.questions.map((q) => ({ sentence: q.sentence, answer: q.answer, ja: q.ja })),
  });
}

io.on("connection", (socket) => {
  socket.on("quiz:createRoom", ({ name }, cb) => {
    const roomCode = makeQuizRoomCode();
    quizRooms[roomCode] = {
      host: socket.id,
      phase: "lobby",
      players: {},
      questions: [],
      startedAt: 0,
    };
    quizJoin(socket, roomCode, name, cb);
  });

  socket.on("quiz:joinRoom", ({ roomCode, name }, cb) => {
    const code = (roomCode || "").toUpperCase().trim();
    const room = quizRooms[code];
    if (!room) return cb({ error: "ルームが見つかりません" });
    if (room.phase !== "lobby") return cb({ error: "テストはすでに開始しています" });
    quizJoin(socket, code, name, cb);
  });

  socket.on("quiz:startGame", ({ category, seriesIndex }) => {
    const roomCode = socket.data.quizRoomCode;
    const room = quizRooms[roomCode];
    if (!room || room.host !== socket.id || room.phase !== "lobby") return;
    const cat = WORDTESTS[category];
    const series = cat && cat.series[Number(seriesIndex)];
    if (!series) return;
    room.questions = shuffle(series.items).slice(0, QUIZ_QUESTION_COUNT);
    room.phase = "playing";
    room.startedAt = Date.now();
    for (const p of Object.values(room.players)) {
      p.submittedAt = null;
      p.score = 0;
    }
    io.to(roomCode).emit("quiz:started", {
      setLabel: `${cat.label} ${series.name}`,
      total: QUIZ_QUESTION_COUNT,
      questions: room.questions.map((q) => ({ sentence: q.sentence, hint: q.hint, ja: q.ja })),
    });
  });

  socket.on("quiz:submit", ({ answers }) => {
    const roomCode = socket.data.quizRoomCode;
    const room = quizRooms[roomCode];
    if (!room || room.phase !== "playing") return;
    const player = room.players[socket.id];
    if (!player || player.submittedAt !== null) return;
    const arr = Array.isArray(answers) ? answers : [];
    player.score = room.questions.reduce(
      (n, q, i) => n + ((arr[i] || "").trim().toLowerCase() === q.answer ? 1 : 0),
      0
    );
    player.submittedAt = Date.now();
    const players = Object.values(room.players);
    io.to(roomCode).emit("quiz:submitProgress", {
      submitted: players.filter((p) => p.submittedAt !== null).length,
      total: players.length,
    });
    quizMaybeFinish(roomCode);
  });

  socket.on("quiz:playAgain", () => {
    const roomCode = socket.data.quizRoomCode;
    const room = quizRooms[roomCode];
    if (!room || room.host !== socket.id || room.phase !== "finished") return;
    room.phase = "lobby";
    room.questions = [];
    for (const p of Object.values(room.players)) {
      p.submittedAt = null;
      p.score = 0;
    }
    io.to(roomCode).emit("quiz:backToLobby");
    quizPlayersUpdate(roomCode);
  });

  socket.on("disconnect", () => {
    const roomCode = socket.data.quizRoomCode;
    const room = quizRooms[roomCode];
    if (!room) return;
    delete room.players[socket.id];
    if (Object.keys(room.players).length === 0) {
      delete quizRooms[roomCode];
      return;
    }
    if (room.host === socket.id) room.host = Object.keys(room.players)[0];
    quizPlayersUpdate(roomCode);
    if (room.phase === "playing") quizMaybeFinish(roomCode);
  });

  function quizJoin(sock, roomCode, name, cb) {
    const room = quizRooms[roomCode];
    sock.join(roomCode);
    sock.data.quizRoomCode = roomCode;
    room.players[sock.id] = {
      name: (name || "名無し").slice(0, 12),
      submittedAt: null,
      score: 0,
    };
    cb({ roomCode, isHost: room.host === sock.id });
    quizPlayersUpdate(roomCode);
  }
});
```

注意: `shuffle()` は server.js の並べ替えバトル部（241行付近）にモジュールスコープで定義済み。再定義しないこと。

- [ ] **Step 4: テストを通す**

Run: `node scripts/test-quiz-flow.js`
Expected: 全行 `ok:` で `ALL PASS`、exit 0

- [ ] **Step 5: 既存ゲームが壊れていないことを確認**

Run: `node -e "require('./server.js'); setTimeout(()=>process.exit(0), 500)"`
Expected: `Server running on http://localhost:3000` が表示されて正常終了（起動時クラッシュなし）

- [ ] **Step 6: Commit**

```bash
git add server.js scripts/test-quiz-flow.js
git commit -m "単語テスト: サーバーのquizイベント群と通しフローテストを追加"
```

---

### Task 6: クライアント `public/quiz.html`

**Files:**
- Create: `public/quiz.html`

**Interfaces:**
- Consumes: Task 5のプロトコル全部（イベント名・payloadは Task 5 の Interfaces を正とする）
- Produces: `/quiz.html?mode=join` と `/quiz.html?mode=create` で初期画面が切り替わるページ（Task 7のリンク先）

- [ ] **Step 1: quiz.html を作成**

デザイントークン・部品スタイル（`:root`変数、`body`、`.card`、`input`、`button`、`.room-code`、`.player-list`、`.error`）は `public/coin.html` の1〜100行と同一のものを使う。以下が完全なコード:

```html
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>📚 単語テスト — シリーズ別5問</title>
<style>
  :root {
    --background: oklch(98.5% .002 247);
    --foreground: oklch(14.5% .02 265);
    --card: oklch(100% 0 0);
    --card-foreground: oklch(14.5% .02 265);
    --primary: oklch(45% .18 265);
    --primary-foreground: oklch(98.5% .002 247);
    --secondary: oklch(96% .01 265);
    --muted-foreground: oklch(50% .02 265);
    --accent: oklch(94% .02 265);
    --destructive: oklch(57.7% .245 27.325);
    --border: oklch(90% .01 265);
    --green: oklch(62.7% .194 149.214);
    --gold: oklch(79.5% .184 86.047);
    --radius: .625rem;
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    font-family: ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
    background: linear-gradient(135deg, var(--background), var(--background) 60%, oklch(45% .18 265 / .05));
    color: var(--foreground);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    padding: 16px;
    -webkit-font-smoothing: antialiased;
  }
  .container { width: 100%; max-width: 42rem; }
  .screen { display: none; }
  .screen.active { display: block; }
  h1 { text-align: center; font-size: 1.875rem; font-weight: 700; letter-spacing: -.025em; margin: 24px 0 8px; }
  .subtitle { text-align: center; color: var(--muted-foreground); font-size: .875rem; margin-bottom: 24px; }
  .card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: .75rem;
    box-shadow: 0 1px 3px 0 rgb(0 0 0 / .1), 0 1px 2px -1px rgb(0 0 0 / .1);
    padding: 24px;
    margin-bottom: 16px;
  }
  label { font-size: .875rem; font-weight: 500; display: block; margin-bottom: 6px; }
  input, select {
    width: 100%;
    height: 2.5rem;
    padding: 0 12px;
    border-radius: calc(var(--radius) - 2px);
    border: 1px solid var(--border);
    background: var(--card);
    color: var(--foreground);
    font-size: .95rem;
    margin-bottom: 14px;
  }
  input:focus, select:focus { outline: 2px solid var(--primary); outline-offset: 1px; }
  button {
    width: 100%;
    height: 2.75rem;
    border: none;
    border-radius: calc(var(--radius) - 2px);
    font-size: .95rem;
    font-weight: 500;
    cursor: pointer;
    background: var(--primary);
    color: var(--primary-foreground);
    transition: opacity .15s, transform .1s;
  }
  button:hover { opacity: .9; }
  button:active { transform: scale(.98); }
  button.secondary {
    background: var(--card);
    color: var(--foreground);
    border: 1px solid var(--border);
    margin-top: 10px;
  }
  button:disabled { opacity: .5; cursor: default; pointer-events: none; }
  .error { color: var(--destructive); text-align: center; margin-top: 8px; min-height: 1.2em; font-size: .875rem; }
  .room-code { text-align: center; font-size: 2.25rem; letter-spacing: .3em; font-weight: 700; color: var(--primary); margin: 4px 0 12px; }
  .player-list { list-style: none; margin: 4px 0 16px; }
  .player-list li {
    padding: 10px 14px;
    background: var(--secondary);
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) - 2px);
    margin-bottom: 6px;
    font-size: .9rem;
    display: flex;
    justify-content: space-between;
  }
  .leave { background: none; border: none; color: var(--muted-foreground); font-size: .85rem; width: auto; height: auto; margin: 16px auto 0; display: block; text-decoration: underline; cursor: pointer; }

  /* テスト画面 */
  .progressbar { height: 8px; background: var(--secondary); border-radius: 9999px; overflow: hidden; margin: 8px 0 4px; }
  .progressbar .fill { height: 100%; background: var(--primary); border-radius: 9999px; transition: width .25s; }
  .progress-label { display: flex; justify-content: space-between; color: var(--muted-foreground); font-size: .8rem; margin-bottom: 16px; }
  .q-ja { font-size: 1.05rem; font-weight: 600; text-align: center; margin-bottom: 6px; }
  .q-hint { text-align: center; color: var(--muted-foreground); font-family: ui-monospace, monospace; letter-spacing: .15em; margin-bottom: 14px; }
  .q-sentence { font-size: 1.15rem; line-height: 1.7; text-align: center; margin-bottom: 18px; overflow-wrap: break-word; }
  .q-sentence .blank { font-weight: 700; color: var(--primary); }
  .answer-input { font-size: 1.1rem; height: 3rem; text-align: center; }
  .nav-row { display: flex; gap: 10px; margin-top: 4px; }
  .nav-row button.back { width: 35%; background: var(--card); color: var(--foreground); border: 1px solid var(--border); }

  /* 待機・結果 */
  .big-status { text-align: center; font-size: 1.6rem; font-weight: 700; margin: 24px 0; }
  .winner { text-align: center; margin: 8px 0 20px; }
  .winner .trophy { font-size: 3rem; }
  .winner .name { font-size: 1.5rem; font-weight: 700; }
  .winner .score { color: var(--muted-foreground); }
  .rank-list { list-style: none; }
  .rank-list li {
    display: flex; justify-content: space-between; align-items: center;
    padding: 10px 14px; background: var(--secondary);
    border: 1px solid var(--border); border-radius: calc(var(--radius) - 2px);
    margin-bottom: 6px; font-size: .95rem;
  }
  .rank-list .me { outline: 2px solid var(--primary); }
  .review-item { padding: 12px 0; border-bottom: 1px solid var(--border); font-size: .95rem; line-height: 1.6; }
  .review-item:last-child { border-bottom: none; }
  .review-item .ok { color: var(--green); font-weight: 600; }
  .review-item .ng { color: var(--destructive); font-weight: 600; }
</style>
</head>
<body>
<div class="container">
  <h1>📚 単語テスト</h1>
  <p class="subtitle">シリーズを選んで5問。全員提出でランキング発表</p>

  <!-- 参加/作成 -->
  <div class="screen active" id="screen-entry">
    <div class="card">
      <label for="name">名前</label>
      <input id="name" maxlength="12" placeholder="ニックネーム" />
      <div id="join-fields">
        <label for="room">ルームコード</label>
        <input id="room" maxlength="4" placeholder="例: AB3K" style="text-transform: uppercase;" />
      </div>
      <button id="btn-enter">参加する</button>
      <p class="error" id="entry-error"></p>
    </div>
    <button class="leave" onclick="location.href='/'">← ゲーム選択に戻る</button>
  </div>

  <!-- ロビー -->
  <div class="screen" id="screen-lobby">
    <div class="card">
      <p style="text-align:center; font-size:.85rem; color:var(--muted-foreground);">ルームコード</p>
      <p class="room-code" id="lobby-code"></p>
      <ul class="player-list" id="lobby-players"></ul>
      <div id="host-controls" style="display:none;">
        <label for="sel-category">単語帳</label>
        <select id="sel-category">
          <option value="clacel">Clacel 2.0</option>
          <option value="ielts">IELTS</option>
          <option value="toeic">TOEIC</option>
        </select>
        <label for="sel-series">シリーズ</label>
        <select id="sel-series"></select>
        <button id="btn-start">テスト開始</button>
      </div>
      <p id="wait-host" style="text-align:center; color:var(--muted-foreground); display:none;">
        ホスト（<span id="host-name"></span>）の開始を待っています…
      </p>
    </div>
    <button class="leave" onclick="location.href='/'">退出する</button>
  </div>

  <!-- テスト -->
  <div class="screen" id="screen-quiz">
    <div class="card">
      <p style="text-align:center; font-size:.85rem; color:var(--muted-foreground);" id="quiz-set"></p>
      <div class="progressbar"><div class="fill" id="bar"></div></div>
      <p class="progress-label"><span id="q-num"></span><span id="submit-status"></span></p>
      <p class="q-ja" id="q-ja"></p>
      <p class="q-hint" id="q-hint"></p>
      <p class="q-sentence" id="q-sentence"></p>
      <input class="answer-input" id="answer" autocomplete="off" autocapitalize="off" placeholder="単語を入力" />
      <div class="nav-row">
        <button class="back" id="btn-back">← 戻る</button>
        <button id="btn-next">次へ</button>
      </div>
    </div>
  </div>

  <!-- 待機 -->
  <div class="screen" id="screen-waiting">
    <div class="card">
      <p class="big-status">提出しました 🎉</p>
      <p style="text-align:center; color:var(--muted-foreground);">
        提出済み <span id="waiting-count"></span> — 全員の提出を待っています…
      </p>
    </div>
  </div>

  <!-- 結果 -->
  <div class="screen" id="screen-results">
    <div class="card">
      <div class="winner">
        <div class="trophy">🏆</div>
        <div class="name" id="winner-name"></div>
        <div class="score" id="winner-score"></div>
      </div>
      <ul class="rank-list" id="rank-list"></ul>
    </div>
    <div class="card">
      <p style="font-weight:600; margin-bottom:8px;">答え合わせ</p>
      <div id="review"></div>
    </div>
    <div class="card" id="again-card" style="display:none;">
      <button id="btn-again">もう一度（ロビーに戻る）</button>
    </div>
    <button class="leave" onclick="location.href='/'">退出する</button>
  </div>
</div>

<script src="/socket.io/socket.io.js"></script>
<script>
  const socket = io();
  const $ = (id) => document.getElementById(id);
  const SERIES_COUNT = { clacel: 8, ielts: 5, toeic: 5 };
  const mode = new URLSearchParams(location.search).get("mode") === "create" ? "create" : "join";

  let isHost = false;
  let questions = [];
  let answers = [];
  let idx = 0;

  function show(id) {
    document.querySelectorAll(".screen").forEach((s) => s.classList.remove("active"));
    $(id).classList.add("active");
  }

  // --- 参加/作成 ---
  if (mode === "create") {
    $("join-fields").style.display = "none";
    $("btn-enter").textContent = "ルームを作成";
  }
  $("btn-enter").addEventListener("click", () => {
    const name = $("name").value.trim();
    if (!name) return ($("entry-error").textContent = "名前を入力してください");
    const done = (res) => {
      if (res.error) return ($("entry-error").textContent = res.error);
      isHost = res.isHost;
      $("lobby-code").textContent = res.roomCode;
      show("screen-lobby");
    };
    if (mode === "create") socket.emit("quiz:createRoom", { name }, done);
    else socket.emit("quiz:joinRoom", { roomCode: $("room").value, name }, done);
  });

  // --- ロビー ---
  function fillSeries() {
    const n = SERIES_COUNT[$("sel-category").value];
    $("sel-series").innerHTML = Array.from({ length: n }, (_, i) =>
      `<option value="${i}">Series ${i + 1}</option>`).join("");
  }
  $("sel-category").addEventListener("change", fillSeries);
  fillSeries();

  socket.on("quiz:playersUpdate", ({ hostId, hostName, players }) => {
    isHost = hostId === socket.id;
    $("host-controls").style.display = isHost ? "" : "none";
    $("wait-host").style.display = isHost ? "none" : "";
    $("host-name").textContent = hostName;
    $("lobby-players").innerHTML = players.map((p) =>
      `<li><span>${p.name}${p.id === hostId ? " 👑" : ""}</span><span>${p.submitted ? "提出済み ✅" : ""}</span></li>`).join("");
    $("again-card").style.display = isHost ? "" : "none";
  });

  $("btn-start").addEventListener("click", () => {
    socket.emit("quiz:startGame", {
      category: $("sel-category").value,
      seriesIndex: Number($("sel-series").value),
    });
  });

  // --- テスト ---
  socket.on("quiz:started", ({ setLabel, total, questions: qs }) => {
    questions = qs;
    answers = Array(total).fill("");
    idx = 0;
    $("quiz-set").textContent = setLabel;
    $("submit-status").textContent = "";
    renderQuestion();
    show("screen-quiz");
    $("answer").focus();
  });

  function renderQuestion() {
    const q = questions[idx];
    const total = questions.length;
    $("bar").style.width = `${(idx / total) * 100}%`;
    $("q-num").textContent = `第${idx + 1}問 / 全${total}問`;
    $("q-ja").textContent = q.ja;
    $("q-hint").textContent = q.hint;
    $("q-sentence").innerHTML = q.sentence.replace("___", '<span class="blank">___</span>');
    $("answer").value = answers[idx];
    $("btn-back").disabled = idx === 0;
    $("btn-next").textContent = idx === total - 1 ? "提出する" : "次へ";
  }

  function saveCurrent() { answers[idx] = $("answer").value; }

  $("btn-back").addEventListener("click", () => {
    saveCurrent();
    idx--;
    renderQuestion();
    $("answer").focus();
  });

  $("btn-next").addEventListener("click", () => {
    saveCurrent();
    if (idx < questions.length - 1) {
      idx++;
      renderQuestion();
      $("answer").focus();
    } else {
      socket.emit("quiz:submit", { answers });
      $("waiting-count").textContent = "…";
      show("screen-waiting");
    }
  });

  $("answer").addEventListener("keydown", (e) => {
    if (e.key === "Enter") $("btn-next").click();
  });

  socket.on("quiz:submitProgress", ({ submitted, total }) => {
    $("waiting-count").textContent = `${submitted} / ${total}人`;
    $("submit-status").textContent = `提出済み ${submitted}/${total}`;
  });

  // --- 結果 ---
  socket.on("quiz:results", ({ ranking, review }) => {
    const top = ranking[0];
    $("winner-name").textContent = top.name;
    $("winner-score").textContent = `${top.score} / ${top.total}問正解 (${(top.timeMs / 1000).toFixed(1)}秒)`;
    $("rank-list").innerHTML = ranking.map((r, i) =>
      `<li class="${r.id === socket.id ? "me" : ""}">
        <span>${i + 1}位 ${r.name}</span>
        <span>${r.score}/${r.total}問 ・ ${(r.timeMs / 1000).toFixed(1)}秒</span>
      </li>`).join("");
    $("review").innerHTML = review.map((r, i) => {
      const mine = (answers[i] || "").trim().toLowerCase();
      const ok = mine === r.answer;
      const mineLabel = mine ? `あなたの答え: ${mine}` : "あなたの答え: （空欄）";
      return `<div class="review-item">
        <div>${i + 1}. ${r.sentence.replace("___", `<strong>${r.answer}</strong>`)}</div>
        <div>${r.ja} — <span class="${ok ? "ok" : "ng"}">${ok ? "正解 ✅" : "不正解 ❌"}</span> <span style="color:var(--muted-foreground)">${ok ? "" : mineLabel}</span></div>
      </div>`;
    }).join("");
    $("again-card").style.display = isHost ? "" : "none";
    show("screen-results");
  });

  $("btn-again").addEventListener("click", () => socket.emit("quiz:playAgain"));
  socket.on("quiz:backToLobby", () => show("screen-lobby"));
</script>
</body>
</html>
```

- [ ] **Step 2: サーバーを起動してブラウザ2枚で通し確認**

Run: `PORT=3100 npm start`（バックグラウンド起動）

ブラウザで確認する項目:
1. `http://localhost:3100/quiz.html?mode=create` — 名前入力→作成→ロビーにルームコード表示
2. 別タブで `http://localhost:3100/quiz.html?mode=join` — 名前＋コードで参加→両タブの参加者一覧に2人
3. ホストで「IELTS / Series 1」を選び開始→両タブに同じ5問・プログレスバー・日本語・ヒントが出る
4. タブ1が5問入力して提出→待機画面「提出済み 1/2人」、タブ2のテスト画面にも「提出済み 1/2」
5. タブ2も提出→両タブにランキング＋答え合わせ（自分の誤答が赤で出る）
6. ホストの「もう一度」→両タブがロビーに戻る
7. 反証: 存在しないコード`ZZZZ`で参加→「ルームが見つかりません」

- [ ] **Step 3: Commit**

```bash
git add public/quiz.html
git commit -m "単語テスト: クライアント画面を追加（1画面1問・プログレスバー・答え合わせ）"
```

---

### Task 7: トップページのリンク差し替え

**Files:**
- Modify: `public/index.html:86-91`（単語テストカードのみ）

**Interfaces:**
- Consumes: `/quiz.html?mode=join` / `/quiz.html?mode=create`（Task 6）

- [ ] **Step 1: 単語テストカードを書き換える**

変更前（86〜91行）:

```html
    <div class="card">
      <p class="game-title"><span class="emoji">📚</span>単語テスト</p>
      <p class="desc">B2レベル5問の穴埋めテスト。全員提出後にランキング発表。</p>
      <a class="btn" href="https://realtime-chat-production-b389.up.railway.app/join/word-quiz">参加する</a>
      <a class="btn secondary" href="https://realtime-chat-production-b389.up.railway.app/admin/word-quiz">ルームを作成</a>
    </div>
```

変更後:

```html
    <div class="card">
      <p class="game-title"><span class="emoji">📚</span>単語テスト</p>
      <p class="desc">Clacel 2.0 / IELTS / TOEICのシリーズを選んで5問の穴埋めテスト。全員提出後にランキング発表。</p>
      <a class="btn" href="/quiz.html?mode=join">参加する</a>
      <a class="btn secondary" href="/quiz.html?mode=create">ルームを作成</a>
    </div>
```

**Surviveカード（92〜97行）の外部リンクは変更しない。**

- [ ] **Step 2: 影響範囲の確認（旧リンクの残りがないこと）**

Run: `grep -rn "word-quiz" public/ server.js`
Expected: ヒット0件（Surviveの`/join/survive`・`/admin/survive`は残っていてよい）

- [ ] **Step 3: ブラウザ確認**

`http://localhost:3100/` を開き、単語テストカードの「参加する」「ルームを作成」がローカルのquiz.htmlに遷移することを確認。

- [ ] **Step 4: Commit**

```bash
git add public/index.html
git commit -m "トップページ: 単語テストをローカル実装に差し替え"
```

---

### Task 8: 最終検証

**Files:** なし（検証のみ）

- [ ] **Step 1: 自動テストを全部通す**

Run:
```bash
node scripts/validate-wordtests.js wordtests-clacel.js 20,20,20,20,20,20,20,12 && \
node scripts/validate-wordtests.js wordtests-ielts.js 20,20,20,20,20 && \
node scripts/validate-wordtests.js wordtests-toeic.js 20,20,20,20,20 && \
node scripts/test-quiz-flow.js
```
Expected: 3つの`OK:`と`ALL PASS`

- [ ] **Step 2: 3ゲーム全部の起動確認**

`PORT=3100 npm start` でサーバーを起動し、`/coin.html?mode=create`・`/order.html?mode=create`・`/quiz.html?mode=create` がそれぞれルーム作成まで動くことを確認（単語テスト追加で既存2ゲームが壊れていないこと）。

- [ ] **Step 3: fable-checkスキルを実行**

完了報告の前に `fable-check` スキルのチェックリスト（リネーム漏れ・呼び出し元・エッジケース・ドキュメント矛盾・デバッグ残骸）を通す。

